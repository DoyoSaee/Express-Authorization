// Placeholder Posts controller for future expansion
// Not currently used by the Passport demo app

async function listPosts(req, res) {
  res.status(501).json({ message: "Not implemented" });
}

module.exports = {
  listPosts,
};

