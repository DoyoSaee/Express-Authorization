<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Welcome · Passport</title>
    <link rel="stylesheet" href="/static/styles.css" />
  </head>
  <body>
    <main class="container">
      <section class="auth-card" aria-label="로그인 성공">
        <header class="auth-header">
          <div class="brand">
            <img class="brand-logo" src="/static/Doyosae.png" alt="Doyosae" />
            <div>
              <h1 class="auth-title">환영합니다!</h1>
              <p class="auth-subtitle">
                <% if (user) { %>
                <strong><%= (user.name || user.email || '사용자') %></strong>
                님, 로그인되었습니다. <% } else { %> 로그인 세션을 찾을 수 없습니다. <% }
                %>
              </p>
            </div>
          </div>
        </header>
        <div class="auth-body">
          <div class="form" style="grid-template-columns: 1fr 1fr">
            <a class="btn btn-primary" href="/">홈으로</a>
            <form action="/logout" method="post">
              <button type="submit" class="btn">로그아웃</button>
            </form>
          </div>
        </div>
      </section>
    </main>
  </body>
</html>
